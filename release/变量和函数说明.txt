变量：
o 加载时，核心脚本物体的缓冲。
p 题库物体。
c 没找到的题数的计数器。
d 题库错误次数的计数器。
f 自动选择没找到和题库错误的题的第一个选项的开关。
i 循环：当前题数（0~99）。
l 问题的innerHTML缓冲。
n 问题名称。
a 问题的返回（从题库函数）。
j 循环：当前选项（0~4）。
s 选项内容。
e 错误信息。

函数：
b(q) 题库函数。
  q 问题名称。
z(x, y) 选择函数。
  x 问题位置（0~99）。
  y 选项位置（0~3）。
k() 启动脚本。
